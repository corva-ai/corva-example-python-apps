git+git://github.com/corva-ai/python-sdk@feature/dc-1236_simplify-app-testing#egg=corva_sdk
lasio==0.28
# lasio depends on numpy, but doesn't specify supported version ranges, which is error prone.
# Lock numpy version to avoid errors.
numpy==1.20.1
pydantic==1.8.1
requests==2.25.1
